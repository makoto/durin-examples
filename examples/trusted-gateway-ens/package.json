{
  "scripts": {
    "compile": "npx hardhat compile",
    "deploy": "npx hardhat run --network localhost scripts/deploy.js",
    "start": "npx hardhat run --network localhost server.js"
  },
  "dependencies": {
    "@nomiclabs/hardhat-ethers": "^2.0.2",
    "@nomiclabs/hardhat-waffle": "^2.0.1",
    "dotenv": "^10.0.0",
    "ens-contracts-attestation-resolver": "ensdomains/ens-contracts#attestation-resolver",
    "envfile": "^6.17.0",
    "ethers": "^5.4.3",
    "hardhat": "^2.6.0",
    "hardhat-deploy": "^0.8.11",
    "makotodurin": "^0.1.0"
  }
}
